{{- if .Values.esoInit.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: test-vault-backend
  namespace: {{ .Values.esoInit.namespace }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded
spec:
  provider:
    vault:
      server: "http://vault.{{ .Release.Namespace }}:8200"
      path: "kv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "eso-reader"
          serviceAccountRef:
            name: {{ .Values.esoInit.serviceAccount.name | quote }}
          secretRef:
            name: "{{ .Values.esoInit.serviceAccount.name }}-token"
            key: "token"
            namespace: {{ .Values.esoInit.namespace | quote }}
{{- end }}

{{- if .Values.esoInit.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.esoInit.serviceAccount.name | quote }}
  namespace: {{ .Values.esoInit.namespace }}
---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Values.esoInit.serviceAccount.name }}-token"
  namespace: {{ .Values.esoInit.namespace }}
  annotations:
    kubernetes.io/service-account.name: {{ .Values.esoInit.serviceAccount.name | quote }}
type: kubernetes.io/service-account-token
{{- end }}
